"use strict";
let gender;
gender = "male";
let isClickable = true;
let aNumber = 7; // TS would auto assign type to variable when define and give value at the same time
function sum(a, b) {
    return (a + b).toString();
}
console.log(sum(12, 34));
